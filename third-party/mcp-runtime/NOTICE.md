# Bundled MCP runtime notices

The generated `friendly-adversary-mcp.cjs` bundle contains these pinned third-party packages:

- `@modelcontextprotocol/server` 2.0.0
- `@modelcontextprotocol/core` 2.0.0
- `zod` 4.4.3

The Model Context Protocol packages use the transition license text stored in `MCP-SDK-LICENSE.txt`. Zod is MIT licensed under `ZOD-LICENSE.txt`. These components retain their original copyrights and license terms.
